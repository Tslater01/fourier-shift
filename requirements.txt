# core — everything except src/generate_fakes.py runs on these.
# pinned to the versions this project was developed and run against (Python 3.12, Windows 11, CUDA 12.8).
#
# torch/torchvision are pinned to CUDA 12.8 builds and are NOT on PyPI. install them first, from the
# pytorch index, then install the rest of this file:
#
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# for a different CUDA version (or CPU-only), use the selector at https://pytorch.org and drop the
# +cu128 local version below. everything else in this file is platform-independent.

torch==2.11.0+cu128
torchvision==0.26.0+cu128

numpy==2.5.1
pillow==12.3.0
scikit-learn==1.9.0
matplotlib==3.11.1
pandas==3.0.5
tqdm==4.70.0
kagglehub==1.0.2

# optional — required ONLY by src/generate_fakes.py, which samples the DDPM cross-generator
# evaluation set. skip these if you are using the pre-generated data/ddpm_fakes/ or are not
# reproducing the cross-generator condition. they pull in a large transitive tree
# (huggingface-hub, safetensors, tokenizers) that nothing else in the project touches.
#
# diffusers==0.39.0
# transformers==5.14.1
# accelerate==1.14.0
